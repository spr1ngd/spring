#pragma once

namespace spring 
{
	class Resources 
	{
	private:
		// special resource load.
	public:
		template<typename T>
		static T Load(const char* resource) 
		{
			
		}

		template<typename T>
		static T* LoadAll( const char resourceFolder )
		{

		}
	};
}
